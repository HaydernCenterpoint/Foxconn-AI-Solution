<Window x:Class="PLC.Views.ConfigImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Import cấu hình máy"
        Width="650" Height="500"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackground}">
    <Window.Resources>
        <Style TargetType="Button" x:Key="WindowBtnStyle">
            <Setter Property="Background" Value="{DynamicResource PanelBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="12,0,12,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Background="{TemplateBinding Background}"
                                Name="border">
                            <ContentPresenter Content="{TemplateBinding Content}"
                                              Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource SidebarBackground}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Button" x:Key="SuccessBtnStyle" BasedOn="{StaticResource WindowBtnStyle}">
            <Setter Property="Background" Value="{DynamicResource AccentColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </Window.Resources>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top: file picker -->
        <Border Grid.Row="0" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}"
                CornerRadius="4" Padding="10" Margin="0,0,0,10"
                Background="{DynamicResource PanelBackground}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Name="TxtFilePath" Text="Chưa chọn file..."
                               Foreground="{DynamicResource TextSecondary}" FontSize="13"/>
                    <TextBlock Name="TxtFileFormat" Text="Hỗ trợ: CSV (tags, alarms) hoặc JSON (full config)"
                               Foreground="{DynamicResource TextMuted}" FontSize="11" Margin="0,3,0,0"/>
                </StackPanel>
                <Button Grid.Column="1" Name="BtnBrowse" Content="Chọn file..."
                        Width="120" Height="32" Margin="10,0,0,0"
                        Style="{StaticResource WindowBtnStyle}"/>
            </Grid>
        </Border>

        <!-- Preview -->
        <Border Grid.Row="1" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}"
                CornerRadius="4" Padding="10" Margin="0,0,0,10"
                Background="{DynamicResource PanelBackground}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Name="TxtPreviewTitle" Text="XEM TRƯỚC DỮ LIỆU"
                           FontWeight="SemiBold" FontSize="14" Margin="0,0,0,10"/>

                <!-- Summary stats -->
                <Grid Grid.Row="1" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Name="TxtTotal" Text="Tổng: 0" FontSize="13"
                                   Foreground="{DynamicResource TextPrimary}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <TextBlock Name="TxtStatus" Text="Status: 0" FontSize="13"
                                   Foreground="{DynamicResource TextPrimary}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Name="TxtProduction" Text="Production: 0" FontSize="13"
                                   Foreground="{DynamicResource TextPrimary}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="3">
                        <TextBlock Name="TxtAlarms" Text="Alarms: 0" FontSize="13"
                                   Foreground="{DynamicResource TextPrimary}"/>
                    </StackPanel>
                </Grid>

                <!-- Preview table -->
                <DataGrid Grid.Row="2" Name="DgPreview" AutoGenerateColumns="True"
                          IsReadOnly="True" GridLinesVisibility="None"
                          Background="Transparent" BorderThickness="0"
                          Foreground="{DynamicResource TextPrimary}"/>
            </Grid>
        </Border>

        <!-- Bottom buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="BtnCreateNew" Content="+ Tạo máy mới từ file này"
                    Width="180" Height="34" Margin="0,0,10,0" IsEnabled="False"
                    Style="{StaticResource SuccessBtnStyle}"/>
            <Button Name="BtnApply" Content="Áp dụng vào máy hiện tại"
                    Width="180" Height="34" Margin="0,0,10,0" IsEnabled="False"
                    Style="{StaticResource SuccessBtnStyle}"/>
            <Button Name="BtnCancel" Content="Hủy" Width="100" Height="34"
                    Style="{StaticResource WindowBtnStyle}"/>
        </StackPanel>
    </Grid>
</Window>
