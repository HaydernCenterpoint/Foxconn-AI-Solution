{
  "name": "FII Factory REST Bridge",
  "description": "Read-only MCP tools for the FII factory backend REST API. This server never connects directly to the factory database.",
  "command": "python",
  "args": ["mcp_servers/plc_mcp_server.py"],
  "env": {
    "MKZ_BACKEND_URL": "http://127.0.0.1:5165"
  },
  "tools": [
    {
      "name": "mkz_get_machines",
      "description": "Get machines from the FII backend",
      "read_only": true
    },
    {
      "name": "mkz_get_production_lines",
      "description": "Get production lines from the FII backend",
      "read_only": true
    },
    {
      "name": "mkz_get_alarms",
      "description": "Get alarm records from the FII backend",
      "read_only": true
    },
    {
      "name": "mkz_get_dashboard_summary",
      "description": "Get dashboard KPIs and machine/alarm totals",
      "read_only": true
    },
    {
      "name": "mkz_get_production_report",
      "description": "Get production report data by time range, line, or machine",
      "read_only": true
    },
    {
      "name": "mkz_get_telemetry",
      "description": "Get live or recent telemetry from the FII backend",
      "read_only": true
    },
    {
      "name": "mkz_get_system_info",
      "description": "Get REST bridge routing information",
      "read_only": true
    }
  ],
  "setup_instructions": [
    "1. Start the FII .NET backend at http://127.0.0.1:5165.",
    "2. This template omits MKZ_BACKEND_TOKEN so the subprocess inherits a configured read-only token from its parent environment.",
    "3. Add this command and environment configuration in Odysseus Settings > MCP Servers.",
    "4. Save and reconnect the MCP server."
  ]
}
